
Compiled 2 Solidity files with solc 0.8.28 (evm target: london)

Running Solidity tests


Running Mocha tests


  AuthorityMultiSig
    Super Admin Management
      ✔ should allow super admins to add a new super admin via majority vote
      ✔ should allow super admins to remove a super admin
    Authority Management
      ✔ should allow super admins to add an authority
      ✔ should allow super admins to remove an authority

  EmergencyReporting
    submitEmergencyReport()
      ✔ T1.2.1 — authorized relayer submits; status is immediately Open (0)
      ✔ T1.2.2 — nullifier reuse reverts with NullifierAlreadyUsed()
      ✔ T1.2.3 — non-relayer is rejected with Unauthorized() (76ms)
      ✔ T1.2.4 — empty CID reverts EmptyCID()
      ✔ T1.2.5 — zero reportHash reverts InvalidHash()
    reclassifyEmergency()
      ✔ T1.2.6 — authority reclassifies; isReclassified=true, 30-day penalty set
      ✔ T1.2.7 — penalized citizen cannot submit another emergency (EmergencyReportingLocked)
      ✔ T1.2.8 — penalized citizen CAN submit after penalty expires (30 days later)
      ✔ T1.2.9 — reclassifying an already-reclassified report reverts InvalidState()
    startWork() and resolveEmergency()
      ✔ T1.2.10 — authority claims Open emergency → InProgress
      ✔ T1.2.11 — authority resolves an InProgress emergency → Resolved
      ✔ T1.2.12 — authority can resolve directly from Open (bypasses InProgress)
      ✔ T1.2.13 — startWork on non-Open report reverts InvalidState()

  OpinionPolling
    createOfficialPoll
      ✔ should allow an authorized authority to create a poll
      ✔ should revert if created by a non-authority
      ✔ should revert if deadline is in the past or present
    castVote
      ✔ should record votes correctly and emit VoteCast event
      ✔ should revert if double voting with the same nullifier is attempted
      ✔ should revert if poll has passed deadline
      ✔ should revert if poll is finalized/deactivated
    finalizePoll
      ✔ should deactivate poll and emit PollFinalized
      ✔ should revert if finalizing a poll that does not exist
      ✔ should revert if finalizing a poll that is already inactive

  Reporting
    submitReport()
      ✔ T1.1.1 — authorized relayer submits a report successfully
      ✔ T1.1.2 — non-relayer is rejected with Unauthorized()
      ✔ T1.1.3 — duplicate nullifier is rejected with NullifierAlreadyUsed()
      ✔ T1.1.4 — empty CID is rejected with EmptyCID()
      ✔ T1.1.5 — zero reportHash is rejected with InvalidHash()
      ✔ T1.1.6 — zero nullifier is rejected with InvalidNullifier()
      ✔ T1.1.7 — zero pseudonym is rejected with InvalidPseudonym()
      ✔ T1.1.8 — report count increments with each submission
      ✔ T1.1.9 — phaseDeadline is set on submission
    castValidationVote()
      ✔ T1.1.10 — upvote increments validationUpvotes and emits ValidationVoteCast
      ✔ T1.1.11 — downvote increments validationDownvotes
      ✔ T1.1.12 — same vote nullifier is rejected with NullifierAlreadyUsed()
      ✔ T1.1.13 — same citizen pseudonym cannot vote twice (CitizenAlreadyVoted)
      ✔ T1.1.14 — vote after window expiry triggers lazy finalization (not counted)
      ✔ T1.1.15 — voting reverts when report is not in PendingValidation
    finalizeVotingWindow()
      ✔ T1.1.16 — status → Open when upvotes > downvotes
      ✔ T1.1.17 — status → CommunityRejected when downvotes >= upvotes (tie defaults rejected)
      ✔ T1.1.18 — finalizeVotingWindow reverts when window is still open
      ✔ T1.1.19 — finalizeVotingWindow succeeds exactly after window duration (time travel)
      ✔ T1.1.20 — batchFinalizeVotingWindows finalizes multiple expired reports
    Authority lifecycle (startWork → markAsSolved → PendingVerification)
      ✔ T1.1.21 — authority claims an Open report (startWork → InProgress)
      ✔ T1.1.22 — startWork reverts on wrong status (e.g. PendingValidation)
      ✔ T1.1.23 — authority marks report solved → PendingVerification with phase deadline set
      ✔ T1.1.24 — wrong authority cannot markAsSolved (Unauthorized)
      ✔ T1.1.25 — authority rejects issue → PendingRejectionReview
    castVerificationVote()
      ✔ T1.1.26 — accept vote increments verificationAcceptVotes
      ✔ T1.1.27 — reject vote increments verificationRejectVotes
      ✔ T1.1.28 — after window: accept ≥ reject → Closed
      ✔ T1.1.29 — after window: reject > accept → Reopened
    castRejectionReviewVote()
      ✔ T1.1.30 — uphold vote increments rejectionUpholdVotes
      ✔ T1.1.31 — appeal vote increments rejectionAppealVotes
      ✔ T1.1.32 — uphold ≥ appeal after window → Closed
      ✔ T1.1.33 — appeal > uphold after window → Reopened
    Query functions
      ✔ T1.1.34 — getAllReports returns reports newest-first
      ✔ T1.1.35 — getAllReports with offset skips correct number of reports
      ✔ T1.1.36 — getAllReports with limit > 100 reverts InvalidPagination
      ✔ T1.1.37 — getReportsByCitizen returns only reports for that pseudonym
      ✔ T1.1.38 — getReportCountByCitizen returns correct count
      ✔ T1.1.39 — getReportsByIds returns correct reports in order
      ✔ T1.1.40 — getReport with invalid id reverts InvalidReportId
    Authority action history (getReportActions)
      ✔ T1.1.41 — action log grows correctly across lifecycle (41ms)

  Security & Sybil Attack Prevention
    T6.1 — Submission nullifier replay prevention (100 attempts)
      ✔ should revert all 100 duplicate nullifier replay attempts (291ms)
    T6.2 — Non-relayer cannot directly submit to contract
      ✔ badActor calling submitReport directly is rejected Unauthorized
    T6.3 — Sybil validation vote prevention (same pseudonym, new nullifier)
      ✔ should reject second vote from same citizen pseudonym even with fresh nullifier
    T6.4 — OpinionPolling double-vote prevention
      ✔ should reject second poll vote with the same nullifier
    T6.5 — Emergency abuse prevention via penalty box
      ✔ should block penalized citizen for 30 days
      ✔ should allow citizen to submit again after 30-day penalty expires
    T6.6 — Sybil protection in verification and rejection review phases
      ✔ should reject duplicate verification vote from same pseudonym


  75 passing (5s)


75 passing (75 mocha)

WARNING: hre.network.connect() is deprecated and will be removed in a future version. Use hre.network.create() or hre.network.getOrCreate() instead.
